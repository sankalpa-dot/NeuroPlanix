## Frontend Setup (Windows)

After cloning the repo:

```bash
cd NeuroPlanix/frontend
npm install
npm run dev



---

## 🆘 If `npm install` Fails for Them

Have them send:

```bat
npm -v
node -v
npm install --loglevel verbose
